# Security

脆弱性、個人情報、秘密情報、ローカルパス、意図しないファイルが公開面またはGit履歴に含まれている可能性を見つけた場合、公開Issueへ値そのものを書かないでください。GitHubのリポジトリ所有者へ、公開値を再掲しない方法で対象ページと発見時刻だけを知らせてください。

本ツールは秘密情報を必要としません。認証情報をフロントエンドへ追加しないでください。

## 配信時の防御と制約

HTTPSを強制し、HTMLのCSPは既定の読み込みを拒否して必要な同一サイトのコード・画像と国土地理院の通信先だけを許可します。インラインスクリプト、埋め込みフレーム、フォーム送信は許可しません。地図画像とデータ取得では参照元URLを送らず、解析Workerのデータ取得では認証情報を送らずリダイレクトも拒否します。共有URLを最初に開く通信自体から座標を隠すものではありません。

現在のGitHub Pagesの設定には任意のHTTPレスポンスヘッダーを指定する項目がありません。CSPのHTTPヘッダー、埋め込み元を制限するframe-ancestors／X-Frame-Options、MIME推測を禁止するX-Content-Type-Optionsは追加できていません。これらをHTMLのmetaタグへ書いても代替にはならず、通常の別ファイルWorkerへHTMLのCSPがそのまま適用されるわけでもありません。完全なヘッダー制御には、対応する配信基盤やプロキシへの変更が別途必要です。JavaScriptによるフレーム脱出や無効な設定ファイルを、ヘッダー防御の代わりとは扱いません。

## 自動検査の範囲

公開allowlist、許可済みバイナリの内容・ハッシュ、全Git objectの内容と未引用のファイル名、到達不能なcommitのauthor・committer、注釈付きtagのtaggerも検査します。シンボリックリンク・submodule・検査不能な履歴は拒否します。検出値そのものはログへ出しません。回帰テストは一時的な独立Gitリポジトリ内の架空値を使い、本番履歴へ混入させません。

自動検査は、定義した秘密・メール・ローカルパス等のパターンと許可一覧に基づく検査です。未知の形式の秘密、文脈でしか分からない個人情報、GitHubから取得できない削除済み・非公開データまで漏れなく検出する保証ではありません。公開前の人による内容確認とGitHub側の履歴・ログ・成果物の確認も必要です。
